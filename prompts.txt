Add functionality for modifying the rendering of Hanzi characters:
- import the library pinyin-pro
- for each string of subsequent Hanzi characters, obtain their Pinyin transliteration using the pinyin-pro library
- render the transliteration over the Hanzi characters

Render the annotation also in editing mode.

add a checkbox to the settings of the plugin saying "Display Pinyin" which toggles the rendering of the Pinyin transliteration
