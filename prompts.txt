Add functionality for modifying the rendering of Hanzi characters:
- import the library pinyin-pro
- for each string of subsequent Hanzi characters, obtain their Pinyin transliteration using the pinyin-pro library
- render the transliteration over the Hanzi characters

Render the annotation also in editing mode.

add a checkbox to the settings of the plugin saying "Display Pinyin" which toggles the rendering of the Pinyin transliteration

Add another checkbox "Colorize Pinyin", which is deactivated when the first checkbox is off. If this is on, render the Pinyin in colors, depending on the tone. Add settings for customizing the colors of the 5 tones with defaults as follows:
tone 1 (flat): green
tone 2 (rising): blue
tone 3 (falling-rising): red
tone 4 (falling): black
tone 5 (neutral): tan
Add another checkbox "Colorize Hanzi"

The "Colorize Pinyin" checkbox shall only toggle the colorization of the Pinyin annotation. The colorization of the Hanzi shall be toggled by the "Colorize Hanzi" checkbox.

Ok merge those two settings into a single checkbox "Colorize by tone"

Add another setting called "Increase font size", a listbox with values 0%, 20%, 40%, 60%, 90% and scale up rendered Hanzi and Pinyin by the selected amount. Default shall be 40%

create a module src/dictionary.ts with a function "normalize" that transforms a string by:
- removing everything in parentheses or brackets
- stripping all whitespace
- turning it into lowercase
- substituting u: and v and ü by u

To the same file add a function parse_line which takes a string and performs these operations:
- split the string by slashes into slash_parts
- take the first slash_part and split it by spaces, take the second of these parts as "hanzi"
- run the hanzi string through the pinyin function of the pinyin-pro library with option toneType set to 'none', apply the normalize function to the result, and call that result "npinyin"
- create a list called searchables with npinyin as its first element
- create an empty list called translations
- for each remaining slash_part:
  - append it to the translations list
  - apply the normalize function and append the result to the searchables list
- return a tuple consisting of hanzi, searchables, and translations

Add a function called parse which takes a string, splits it into lines, and for each non-empty line that does not start with # apply the parse_line function, and return a list of the results

Add a function called make_dictionary which takes a URL as a string, downloads the file from that URL, runs parse on its content, and stores the result as json in a file called dictionary.json

add a testcase in an appropriate place called test_parse which runs make_dictionary on the URL https://github.com/gugray/HanDeDict/blob/master/handedict.u8 and run it

Modify parse_line so that it ignores empty slash_parts and run the testcase again

at plugin startup, if dictionary.json is present, load it into memory

add a configurable keyboard shortcut called "Dictionary Lookup", defaulting to CTRL+SHIFT+D.
It shall take the currently selected text and match it against each searchable string plus the hanzi of each dictionary element. Sort all elements where at least one searchable or the hanzi contains the selected text by the length of the matching searchable or hanzi in increasing order. Open a popup menu with a scrollable list of the sorted matching dictionary elements.
Format each element in one row of the popup, with segments for hanyi and each translation.

When running npm run dev, I get this error: The package "node:fs/promises" wasn't found on the file system but is built into node. Are you
  trying to bundle for node? You can use "platform: 'node'" to do that, which will remove this
  error.

Still same error

In the settings page, add a text field "Dictionary Source" pre-filled with the URL https://github.com/gugray/HanDeDict/blob/master/handedict.u8 and a Download button which when clicked runs make_dictionary on the provided URL, and then loads the generated dictionary.json file

When make_dictionary fails, give a more informative error message

It seems to fail when trying to write dictionary.json. Choose an appropriate path that is suitable for plugins to write to

make the layout of the popup more compact vertically: decrease the spacing before and afte the horizontal lines. Rendering the hanzi in the popup shall respect the set options for coloring. Show the pinyin transliteration (colored if so configured) in a new segment between hanzi and the translations.

Do not show hanzi in bold face. Don't show the number of matches.

Make all segments in the popup clickable. When clicked, copy its contents to the clipboard, close the popup, and display feedback what has been copied. In the header of the popup, add a small note "Click to copy to clipboard"

Add a button in the sidebar with the icon "book-a" for dictionary lookup.

When dictionary lookup gets triggered and no text is selected, use the text in the current line instead, stripped of markdown like # and - and [] and so on.

re-write README.md to be a project readme on github

Run the linter and fix all issues.

Change the sorting of the displayed dictionary entries. Sort primary by increasing length of the hanzi string, and secondary by increasing length of the matching searchable. if the query matched the hanzi, use the shortest searchable for secondary sorting.

Change the function parse_line: when turning a slash_part into a searchable string, remove the string ": Bsp.:" and everything that follows before normalizing

Add a file wordlist.ts with a function make_ranking that gets called at build time. The function shall download the file from https://github.com/drkameleon/complete-hsk-vocabulary/blob/main/complete.json and parse its JSON content and process it like this:
- create an empty result dictionary
- go through each vector element in complete.json, which is a dictionary. Take the values of the keys "simplified" and "frequency". Add them to the result dictionary with simplified as the key and frequency as the value.
- store the result dictionary in the file ranking.json in an appropriate path so that it can be read at plugin startup

At plugin startup, load the ranking dictionary in ranking.json. Now when sorting matching dictionary entries, look up the ranking of each matching entrie's hanzi in the ranking, setting it to 1000000 if it doesn't exist in the ranking. Sort by increasing ranking, and decide ties by the former sorting method.

Show the ranking of a dictionary entry in the popup with a small font in the beginning of a row.

Omit showing the ranking for entries which cannot be found in the ranking dictionary

Make sure ranking.json is included when publishing and installing the plugin

Hide the ranking in the popup again

